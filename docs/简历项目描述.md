# 简历项目描述 — AI-Optimized-Resume-V2

> 以下内容可直接复制到简历「项目经历」一栏。

## 版本 A · 标准版（推荐默认）

**个人职业操作系统（Personal Career OS）** | Python · FastAPI · LangGraph · React · SSE  
*2025 – 至今 · 个人项目*

面向 IT 从业者的 AI 职业规划与简历交付平台：以 `profile.json` 长期职业档案为记忆底座，打通「身份建模 → 能力资产 → 市场判断 → 策略推演 → JD 适配 → 可打印 HTML 简历」全链路，本地优先、对话驱动。

- 落地 **一主多从** 多智能体架构（协调者 + 身份/能力/市场/机会/策略/简历/资产 7 类 Worker）：协调者统一意图识别、派工与 SSE 流式合成；Worker 禁止互调，上游结论经 Harness 落档与会话上下文注入下游，实现认知分层解耦，避免「万能用户画像」单 Agent 膨胀。
- 构建 **Harness 运行时**：对话驱动的动态任务系统（milestone/work 强约束、纯对话启停/放弃）、Worker Run 内自选 Skill 按需加载、JD 推荐闸门与优化确认闸门；Tool 契约统一驱动档案读写、HTML 落盘与 `outputs_index` 索引登记。
- 打通 **端到端职业闭环**：职业初探五主题 + `experience_bank` 经历素材库 → 市场情报（联网浏览器 Tool）与 JD 匹配评估 → 多路径策略推演与三时间维度投递叙事 → 保守/标准/进取三档同轮多份 HTML、历史简历复用与 index 资产管理。
- 采用 **Python 单体 + React** 本机部署：FastAPI REST + SSE 逐字流式、LangGraph Agent 编排、跨会话 JSON 长期记忆；简历 HTML 纯正文可直打 PDF，真实档案与产出本地隔离不入 Git。

---

## 版本 B · 精简版（简历空间有限）

**Personal Career OS** | Python · LangGraph · FastAPI · React · SSE | 个人项目 · 2025–至今

AI 职业规划与简历交付 Agent 系统：一主多从 Multi-Agent + Harness 运行时，以 `profile.json` 为长期记忆，对话驱动「初探 → 市场/JD → 策略 → HTML 简历」闭环；本地优先，SSE 流式交互。

- 协调者 + 7 域 Worker 分层派工，Harness 统一 Tool/Skill/Task/存储，Worker 互不调、结论经落档注入下游。
- 三阶段 Skill 包 + 对话闸门（JD 推荐/优化确认）+ 动态任务系统；同轮输出保守/标准/进取三档 HTML 简历。

---

## 版本 C · Agent 开发岗侧重（简历优先 · 定稿稿）

> **用法**：本版本是 **简历定稿**——先写清楚面试官要看到什么、会追问什么，再按文末「简历 → 实现 Checklist」补代码与 Eval。  
> 投递 Agent 应用开发 / LLM 应用工程师时，**优先粘贴下方「简历正文」**。

### 简历正文（可直接粘贴）

**个人职业操作系统（Personal Career OS）** | Python · FastAPI · LangGraph · React · SSE  
*2025 – 至今 · 个人项目*

垂直场景 Multi-Agent 产品：自研 Agent Harness，将 IT 从业者「职业认知 → 市场判断 → JD 策略 → 可打印简历」长流程拆为 **协调者 + 7 域 Worker**，以 `profile.json` 为跨会话长期记忆，对话驱动、本地优先交付。

- 设计并实现 **一主多从调度**：协调者只做意图识别、任务拆分、派工与 SSE 流式合成（不执行业务深挖）；7 类 Worker 职责互斥、**禁止互调**；上游 `structured_output` 经 Harness 落档 / 写入 `session_state` 后注入下游，避免「万能用户画像 Agent」的上下文膨胀与职责漂移。
- 自研 **Agent Harness 运行时**（非 LangChain 薄封装）：Prompt / Skill / Tool / Task / Storage 五类平台服务同进程暴露；Tool 注册表统一驱动档案读写、动态任务图、HTML 落盘与 `outputs_index`；**5+ 对话闸门**（深度探讨、JD 不推荐仍继续、优化确认等）实现 HITL，Task 层 `milestone/work` 强约束流程，Worker **禁止直出 SSE token**。
- 落地 **Context Engineering**：协调者派工仅传 `goal + context + skill_index`（不预加载 Skill 正文）；Worker Run 内 **自选 `load_skill`**（初探 / JD 对齐 / 简历模块化三 Skill），Harness 校验 `allowed_workers` 后按需注入；长期事实按域切片读 `profile.json`，Skill 与 Prompt 分离，新增业务能力 **零改协调者 Prompt**。
- 打通 **端到端职业 Agent 闭环**：五主题初探 + `experience_bank` → Browser Tool 公开市场情报与 JD 评估 → 策略三时间维度推演 → **同轮输出保守 / 标准 / 进取三档 HTML 简历**；历史产出 index 管理与复用，HTML 纯正文可直打 PDF。
- 按 **Eval 驱动迭代**：设计 20+ 条端到端 case（闸门误触发、Tool 失败降级、三档表述一致性、Worker 派工链完整性）；Harness 记录 `delegate / load_skill / tool_call` trace，支持 trajectory 回放与失败归因。

**一句话（空间极限时替换项目描述）**：基于 LangGraph 自研 Harness 的垂直 Multi-Agent 系统，协调者调度 7 域 Worker，对话闸门 + 档案切片 + Skill 按需加载，完成职业规划到三档 HTML 简历的本地闭环。

---

### 三维表达（写 bullet 时的检查清单）

| 维度 | 本项目的答法 |
|:---|:---|
| **架构** | 一主多从 · Harness 五平台服务 · Worker 互不调 · 协调者唯一 SSE 出口 · LangGraph Loop |
| **业务** | IT 职业规划 · 初探 → 市场/JD → 策略 → 三档 HTML · 本地档案沉淀 |
| **结果** | 7 Worker + 3 Skill + 5+ 闸门 + 20+ eval · 可 clone 本地运行 · trace 可复盘 |

---

### 面试官深挖点（每个 bullet 对应 1–2 个必考题）

| 简历 bullet | 面试官 likely 追问 | 你要讲清的核心判断 |
|:---|:---|:---|
| 一主多从 | 为什么不用 AutoGen Group Chat / 单 Agent？ | 认知域不同（身份/能力/市场/策略/交付）；协调者 = 组织里的「管理层」做信息精炼，不是微服务乱拆；Worker 互调会导致 context 复制与职责边界模糊 |
| 协调者不干活 | 协调者和 Worker 边界怎么划？会不会协调者越来越胖？ | 协调者禁止 JD 打分、改简历、写 profile；只做派工 + 合成 + 闸门引导；业务规则进 Skill，工具约束进 Prompt，Harness 管状态 |
| Harness 自研 | 和直接 LangGraph + LangChain Tools 有什么区别？ | Harness = 职业 OS 运行时：Task 强约束、闸门、存储契约、Skill 注册表、审计 trace；不是「调 API 的胶水」 |
| Worker 不直出 SSE | 为什么 Worker 结果不能流式给用户？ | 统一对话入口与产品人格；Worker 出 `structured_output`，协调者控制话术与闸门；避免多 Agent 抢麦 |
| Skill 按需加载 | 为什么协调者不指定 skill_name？ | 同一 Worker 不同任务路径需要不同 Skill 组合；预指定会绑死流程；索引 + Worker 自选 = 更灵活的 Context Engineering |
| 5+ 对话闸门 | HITL 怎么设计？会不会打断体验？ | pre-turn 闸门（进深度探讨前）/ mid-flow（JD 不推荐仍继续）/ pre-delivery（优化确认）；核心版本无独立按钮，自然语言确认 |
| Browser Tool | 联网失败怎么办？ | 公开情报检索 + 来源标注 + 失败降级（仅用 profile 已有 market 数据继续）；不伪造检索结果 |
| 20+ eval | eval 怎么分层？ | component（Tool schema / 闸门触发）+ trajectory（派工链顺序）+ E2E（初探 → 三档 HTML）；golden case 来自真实 JD 与初探对话脚本 |
| 三档 HTML 同轮 | 三档差异是什么？怎么保证不 hallucinate 经历？ | 保守/标准/进取 = 表述力度与 JD 对齐度，不是编造经历；素材来自 `experience_bank` + 用户确认事实；eval 查「无证据断言」 |

---

### 简历 → 实现 Checklist（按优先级对齐代码）

> 简历上写的每一项，面试前都应能 demo 或讲清实现；下列顺序即建议开发顺序。

| 优先级 | 简历 claim | 最低实现标准 |
|:---:|:---|:---|
| P0 | 一主多从 + 协调者 SSE | 协调者 LangGraph 主图 + 至少 2 个 Worker 可 delegate；Worker 只返 structured_output |
| P0 | Harness Tool 注册表 | Profile read/patch、delegate_worker、write_html、outputs_index 登记 |
| P0 | 5+ 对话闸门 | 深度探讨邀请、JD 不推荐仍继续、优化确认 — 至少 3 个可触发可拒绝 |
| P1 | 3 Skill 按需加载 | `.agent/skills` 三包装载 + `allowed_workers` 校验 + Worker 内 load_skill |
| P1 | 端到端闭环 | 初探落档 → mock JD → 策略 → 三档 HTML 同轮落盘 |
| P1 | Worker 不直出 SSE | 代码层禁止 Worker token 事件，仅协调者 stream |
| P2 | Browser Tool | 公开市场检索 + 来源字段 + 超时/失败降级 |
| P2 | Task milestone/work | explore / jd 两类 list；对话 start / abandon |
| P2 | 20+ eval | Promptfoo 或 pytest；覆盖闸门、派工链、三档一致性各 ≥5 条 |
| P2 | trace 回放 | 每步 delegate / skill_load / tool_call 写 JSONL，可 grep 失败原因 |

---

### 与版本 A 的差异

| | 版本 A | 版本 C |
|:---|:---|:---|
| 定位 | 通用标准描述 | **Agent 开发岗定稿**，简历优先 |
| 结果表达 | 偏能力罗列 | 明确 **Eval / trace / 闸门数量** 等可量化锚点 |
| 附带 | 无 | 深挖点 + 实现 Checklist，指导后续补代码 |

---

## 版本 D · 系统设计 / 全栈侧重

**AI 职业规划与简历交付平台** | FastAPI · LangGraph · React 19 · TypeScript · SSE  
*2025 – 至今 · 个人全栈项目*

本地优先的职业 Agent 产品：前端对话 + 表单 + 任务进度 + 简历 index 管理；后端 Python 单进程承载 API、Harness 与全部 Agent Run。

- **前后端边界清晰**：UI 仅 REST/SSE 对接 `career_os`，不直连 LLM、不读写 `profile.json`；SSE 逐字流式 + Worker 结构化输出经协调者合成，Worker 禁止直出用户 token。
- **存储分层**：`profile.json`（长期事实）· `data/tasks/**`（动态任务图）· `output/YYYY-MM-DD/`（HTML + index）· 真实档案本地隔离不入 Git。
- **可打印交付物**：简历 HTML 纯正文结构，浏览器直打 PDF；支持历史简历打开/删除与 index 资产管理。

---

## 版本 E · 个人站点 / GitHub 一句话

> 面向 IT 从业者的 Personal Career OS：LangGraph 一主多从 Multi-Agent + Harness 运行时，对话驱动职业初探、JD 策略对齐与三档 HTML 简历交付，本地优先、SSE 流式、长期档案可沉淀。

**关键词**：Multi-Agent · Agent Harness · Context Engineering · LangGraph · RAG-free 结构化记忆 · HITL 闸门 · SSE · 垂直 Agent 产品

---

## 面试讲述提纲（STAR 骨架）

| 环节 | 要点 |
|:---|:---|
| **Situation** | 通用 ChatBot 无法支撑「长期职业档案 + 多阶段流程 + 可交付 HTML 简历」；单次 JD 匹配不够，需要 Capability Evolution 式沉淀。 |
| **Task** | 设计一套可写进简历、面试能讲清的垂直 Agent 系统：分工清晰、状态可恢复、关键决策有人审、产出可索引。 |
| **Action** | 采用一主多从（协调者 + 7 Worker）；Harness 统一 Tool/Skill/Task/存储；三 Skill 包覆盖初探/JD/简历；对话闸门 + 动态任务；PRD/架构文档驱动实现。 |
| **Result** | 形成完整产品规格与 Harness 设计，Skill 包可运行加载；端到端流程从对话入口到 HTML 交付路径明确，便于后续补 Eval case 与 Demo 上线。（*实现期可替换为：X 条 eval 通过率、平均轮次、token 成本等实测数据*） |

### 面试深水区速答

| 问题 | 回答要点 |
|:---|:---|
| 为什么 Multi-Agent，而不是一个 Agent？ | 认知域不同（身份/能力/市场/策略/交付），单 Agent Prompt 会膨胀且难维护；协调者做信息精炼与派工，对齐「组织分工」而非微服务过度拆分。 |
| Worker 之间如何传上下文？ | **禁止互调**；协调者将上游 `structured_output` 写入 `session_state` 或经 Harness 落档，派下游时注入 `context`。 |
| Harness 管什么、模型管什么？ | Harness 管 Tool 注册、权限/闸门、Task 状态、存储契约、Skill 加载、trace；模型管域内推理与工具选择（含 Worker 内自选 Skill）。 |
| 上下文太长怎么办？ | 长期事实进 `profile.json` 切片注入；Skill 按需加载；Worker 只收 goal+context，不全量灌会话历史；协调者合成时压缩上游结论。 |
| 怎么证明不是 Demo？ | 对话闸门 + Task 强约束 + 结构化落档 + HTML/index 可交付；后续补 20 条 eval（初探完整性、JD 闸门、简历三档一致性、Tool 失败降级）。 |

---

## 简历项目描述公式对照（AgentGuide 三维）

| 维度 | 本项目怎么写 |
|:---|:---|
| **架构** | 协调者 + 7 Worker；Harness Tool/Skill/Task；LangGraph Loop；SSE 流式；Worker 互不调、落档注入 |
| **业务** | IT 职业规划；初探五主题 → 市场/JD → 策略三时间维度 → 三档 HTML 简历；本地档案沉淀 |
| **结果** | 7 Worker + 3 Skill + 5+ 闸门 + 20+ eval · 可 clone 本地运行 · trace 可复盘（见版本 C Checklist） |

---

## 投递时选用建议

| 目标岗位 | 推荐版本 | 强调 |
|:---|:---|:---|
| AI Agent 应用开发 | **版本 C（定稿）** 或 A | Harness、Multi-Agent 调度、HITL 闸门、Eval/trace（C 含深挖点 + Checklist） |
| LLM 应用 / 全栈 | 版本 D | 前后端边界、SSE、存储分层、HTML 交付 |
| 简历一行放不下 | 版本 B | 保留架构 + 闭环两句 |
| GitHub / 个人站 | 版本 E | 一句话 + 关键词 |
| 面试前 30 分钟 | 面试讲述提纲 | STAR + 深水区速答 |
